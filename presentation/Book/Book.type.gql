# represent a book
type Book {
  # the book id
  id: ID!
  # the book title
  name: String!
  # the book author
  author: String
  # the bamer that may have borrowed the book
  currentlyBorrowedBy: Bamer
}