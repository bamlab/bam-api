# represents the map of the fields that will derectly be available on top of the graphQL query
type Query {
  # a bammer, by id
  bammer(id: ID!): Bammer
  # all the bammer
  allBammers: [Bammer]
  # a book, by id
  book(id: ID!): Book
  # all the book
  allBooks: [Book]
}